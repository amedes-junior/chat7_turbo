<p id='<%=dom_id message%>'>
  <%= message.created_at.to_s%>: <%=message.content%> |
  <%=link_to "destroy", room_message_path(message), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>

</p>